import React from 'react'

const Camera = (props) => {
    return (
        <div>
            <h4>{props.camera.model}</h4>
            {/* <buton>Remove</buton> */}
        </div>
    )
}
 export default Camera;